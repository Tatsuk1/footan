<%= form_with(model: [@post, @shop], local: true) do |f| %>
  <%= f.label :content, 'コメント' %>
  <%= f.text_field :content %>

  <%= f.submit '投稿' %>
<% end %>

<%= link_to '戻る', shop_path(@shop.shop_code) %>